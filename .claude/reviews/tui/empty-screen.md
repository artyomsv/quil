# Code Review State: tui / empty-screen

Last reviewed: 2026-08-16
Rounds completed: 1

## Resolved (fixed in code; do not re-raise)
- [code-quality/1 MEDIUM] `splitPane` — the split the KEYBINDINGS and the command palette call, i.e. the split people actually use — armed a placeholder with no `phType`, so it rendered the blank rect the whole change exists to remove. Now sets `placeholder.phType = "terminal"`: the payload carries no `Type` and the daemon normalises an empty one to terminal, so the label is what the pane will be rather than a guess — round 1
- [code-quality/2 MEDIUM] `splitPane` had no in-flight refusal, so a split key pressed during a `git worktree add` armed a SECOND placeholder in the tab. `renderNode` hands the TAB's `CreatingBranch` to every placeholder leaf, so both rendered "Creating worktree <branch>" while the split had no worktree at all — and it overwrote `pendingSplit[tab.ID]`, stranding the leaf the create reserved (that half was pre-existing). Now refuses with the same flash `handleCreatePaneSplit` uses; the one-placeholder-per-tab invariant is what makes the tab-level branch correct — round 1
- [code-quality/3 LOW] `phType`'s "it needs no lifecycle at all" claim was not held by the code. A node that stops being a placeholder is not destroyed — a REPLACE writes the label onto an EXISTING leaf, and `RemoveLeaf` promotes a sibling into a parent IN PLACE — so a stale label reached the screen. `LayoutNode.fill` is now the single choke point for "this placeholder got its pane" (`FillPlaceholder`, `rebuildTabs`, `settleReplacedPane`) and clears the label; `RemoveLeaf` carries `phType` with the other five promoted fields — round 1
- [code-quality/4 LOW] The overlay settle condition claimed to mirror the layout-tree branch but read `preparing` alone where the tree reads `resuming || preparing`. Inert today (no overlay path arms `resuming`) and would have left the next one that does to the 30 s safety cap. Widened, and it now clears both flags — round 1
- [code-quality/5 LOW] The tick-chain START site had no test: every overlay test called `applyWorkspaceState` directly, so nothing drove the `WorkspaceStateMsg` arm of `Update` where the arming loop lives — the bypassed-call-site shape this package has shipped before. `TestWorkspaceState_ArmsTheSpinnerChainForANewOverlay` closes it — round 1
- [security/LOW-1] The new `Starting <type>…` line's `sanitizeRemoteText` + `truncateCells` pair had no test that could reach it (the existing controls test passes `paneType=""`, and the branch outranks the type), so deleting either control left the suite green. `TestRenderPendingPane_SanitizesAndBoundsTheType` pins both — round 1
- [qa/1] `tab.CreatingBranch = ""` on both settle paths was asserted by nothing — deleting either line kept the suite green, readmitting exactly this change's bug class (a split armed after a failed create rendering the branch that just FAILED). Assertions added to `TestReplace_FailedAddPutsTheOldPaneBack` and `TestReplace_TimeoutPutsTheOldPaneBack` — round 1
- [qa/2] The overlay `preparing`-really-clears positive case was untested; only "a blank boot frame does not settle it" was pinned, which a permanently-stuck flag also satisfies. `TestOverlayRealContent_SettlesTheIndicator` added, backdating `resumeStart` past `restoreMinDisplay` the way `restore_indicator_test.go` does — round 1
- [qa/3] `renderPendingPane`'s type-only branch had no tiny-rect coverage — the size sweep always passed a branch, and the branch outranks the type, so a budget mistake specific to the `Starting` line would have shipped. `TestRenderPendingPane_TypeLineFitsItsRectAtEverySize` mirrors the branch-line sweep — round 1
- [rules/1 MEDIUM] `.claude/rules/projects.md` documented `CreatingBranch` as pushed from the single `rebuildTabs` read and knew nothing of `LayoutNode.phType`. Extended with the three-writer mirror (submit / settle / broadcast) and why the submit write is the load-bearing one, the node-local label and its `fill`/`RemoveLeaf` pair, and why `splitPane` owes the in-flight refusal — round 1
- [rules/2 MEDIUM] `.claude/rules/plugins.md` covered the overlay slot exhaustively but not the boot indicator. Extended with the `pendingOverlayShow` gating, `spinnerTargetPane` superseding `leafByID`, and the `restoreSettled()`-gated clear — round 1

## Dismissed (acknowledged, will not fix; agents may escalate with explicit justification)
- [security/pre-existing] `restoreContext()` and `resumeLabel()` (`internal/tui/pane.go`) render `Pane.Type`, `Pane.Name` and `filepath.Base(Pane.CWD)` through `lipgloss.Place` (which pads but never clips) with no `sanitizeRemoteText` and no bound, and all three are copied verbatim from the daemon's `workspace_state` by `syncPaneMeta`. `renderRestoreIndicator`'s `widest+2 > innerW` fallback is not a bound, because `ansi.StringWidth` measures an escape as zero cells. NOT introduced here and NOT widened here: `showRestoreIndicator()` already fires on `Pending` alone, which `syncPaneMeta` copies for overlay panes too, so a hostile remote daemon had this surface either way. Belongs in its own commit across all four fields — tracked as `techdebt/3-2-restore-indicator-renders-unsanitized-daemon-strings.md` (round 1)
- [code-quality/spinnerTickRunning] No direct test for the chain-start guard's `continue` branch (arming twice for one pane). Pre-existing — the loop used `leafByID` before — and `TestWorkspaceState_ArmsTheSpinnerChainForANewOverlay` now covers the arm itself. A second-broadcast test would assert nothing new: an adopted overlay is in `existingPanes`, so it never reaches `newPaneIDs` again (round 1)

## Notes
- Semgrep (`p/owasp-top-ten` + `p/golang`) ran clean over 7 files with coverage verified via `paths.scanned` — inside a git worktree Semgrep's git-based target discovery finds zero files and still exits 0.
- `phType` was traced to local origin before being judged: both write sites assign `pluginName` = `m.selectedPlugin`, set from the LOCAL `m.pluginRegistry`. `Registry.SetAvailability` is the only daemon influence on that registry and it overwrites the `Available` bool alone — it never introduces a daemon-supplied name. The sanitize+bound on that line is defence-in-depth, and is pinned as such.
- `TestOverlayReattach_ClaimsNoBoot` passes under a full revert of this change (`preparing` is false by zero value). It is deliberately a companion negative case — it guards against an over-broad fix that marks EVERY overlay `preparing` — and is not the test that pins the fix; `TestOverlayArrival_ShowsABootIndicator` is.
