# Code Review State: quil / input-history

Last reviewed: 2026-07-31
Rounds completed: 3

## Resolved (fixed in code; do not re-raise)
- [greptile/P1] historyVisibleRows floored at 3 rows, forcing a 10-row box onto terminals with 6-9 rows that had room for a shorter one — lipgloss.Place does not clip, so a floor above the available height manufactures the overflow it looks like it prevents; floor is now 1, and the box exceeds the terminal only below historyChromeRows+1 where the chrome alone does not fit — round 3
- [security/H-1] The history detail viewer rendered recorded prompt text verbatim to the terminal — a full-screen editor is not a pane, so nothing re-renders it through the VT emulator, and a prompt carrying OSC 52 (honoured by several terminals as "set system clipboard") would silently replace the next paste on re-read; the historyEntryMsg arm now runs claudesessions.SanitizePrompt before openReadonlyText — round 3
- [security/M-2] PreviewLine dropped category Cc but not Cf — a bidi override is printable to unicode.IsControl and invisible to unicode.IsSpace, so it survived both the control filter and the whitespace collapse and could make a row read as a different prompt than the one Enter fetches; now drops IsControl + Is(unicode.Cf) — round 3
- [security/M-3] The preview was sanitized daemon-side only, but under `--remote` that is the untrusted end (the version handshake proves the binaries match, not that the peer is honest) and truncateToWidth measures an escape as zero cells so it survives truncation intact; applyHistoryList now re-runs claudesessions.SanitizeTitle on every arriving row — round 3
- [security/L-4] logViewerPosAt's two branches had different contracts (the SoftWrap branch was clamped by visualToLogical, the other returned a row past the end on a click below the last line) — both now clamp through clampPos, so ok==true means "valid document position" for every future caller — round 3
- [code-quality/I-1] No mouse branch in Update had a dialog guard, yet View renders ONLY the dialog while one is open — a left-press inside a modal armed hitTestSplitBorder and the release ran finishSplitDrag, moving a split ratio, resizing two PTYs and persisting the layout invisibly; a press at row 0 switched tabs the same way. modalSwallowsMouse now absorbs those, ordered after viewerOwnsMouse so the read-only viewer keeps its own selection. Pre-existing, affected every dialog — round 3
- [code-quality/I-2] viewerMouseDown was never cleared when the viewer closed, so a drag whose release was dropped outside the terminal window resumed on the next viewer's first hover, anchoring a selection at a document position from a different prompt; newLogViewerEditor took a pointer receiver so clearDragState runs at the one choke point every viewer entry funnels through — round 3
- [code-quality/S-3] scrollViewer had a value receiver while doing the same job as scrollHistoryList three lines away in Update — a later `m.field = x` would land on a discarded copy and no editor-state test would notice; pointer receiver — round 3
- [code-quality/S-4] historyInnerWidth was a third verbatim copy of paletteInnerWidth (setupTextWidth a fourth); the terminal clamp and the dialogBoxChrome subtraction now live once in dialogInnerWidth(termW, boxW), which all four derive from — a drifting copy reintroduces the two-cells-too-wide reflow silently while the other copies' tests stay green — round 3
- [code-quality/S-5] The loading/unsupported/empty status lines were written untruncated while every other line went through truncateToWidth; at minTermWidth the unsupported message is longer than the box and reflowed onto a second row, eating a chrome row the height budget assumes — round 3
- [code-quality/S-6] IsSyntheticPrompt tested each tag's open and close as a PAIR, so a turn carrying both a <task-notification> and an <agent-message> (the normal parallel-subagent case) matched neither rule; the two ends are now matched independently, and the comment plus CLAUDE.md corrected — round 3
- [code-quality/S-7] The list request had no timeout: loading was cleared only by applyHistoryList, so a response that never arrived (dropped, or rejected by DecodePayload, which listenForMessages answers with a bare listenContinueMsg) left the modal on "Loading…" forever; historyReqTimeout (3 s) + `r` to retry, a LOCAL timer that must not re-arm listenForMessages — round 3
- [qa/gap-1] modalSwallowsMouse had a predicate unit test but no dispatch-level test — the predicate returning the right bool proves nothing about the four Update branches, which is where the bug lived; modal_mouse_dispatch_test.go drives real m.Update(tea.Mouse*Msg) calls and asserts splitDragNode/tabDragFromIdx/Root.Ratio/pane scroll are untouched, each paired with a dialogNone control (which immediately caught a wrong coordinate: a rendered tab carries padding its label width does not include) — round 3
- [qa/gap-2] applyHistoryTimeout rejects a tick on three grounds; the stale-pane and already-loaded arms were tested, the closed-dialog one was not (Esc closes the modal without resetting m.history, so an in-flight tick still arrives) — round 3
- [greptile/P1] Compact gated on the RAW line count, so historical synthetic junk could push a few-real-prompt pane over keepLast and trigger a rewrite that races the hook's append — gate now uses the filtered (real) entry count; junk is a finite non-growing residue (Append no longer records it), purged only on a genuine over-cap trim — round 2
- [greptile/P2] A real prompt starting by quoting `<task-notification` was dropped by Append — IsSyntheticPrompt now requires BOTH the open tag AND the `</task-notification>` close tag, so only a pure complete block is filtered; a sentence quoting the tag is preserved — round 2
- [code-quality/M-1] Compact rewrote on every previews read (whenever any synthetic entry was on disk), racing the Claude hook's cross-process O_APPEND and risking lost prompts — Compact now rewrites ONLY when the file exceeds keepLast lines; Read-time filtering keeps the display clean, junk is purged as a side effect of ring eviction — round 1
- [code-quality/L-1] `<task-notification>` prefix was brittle to attribute drift — prefix now omits the closing `>` so `<task-notification version="2">` still matches — round 1
- [code-quality/L-2] Negative keepLast could panic in the trim slice — guarded with `if keepLast < 0 { keepLast = 0 }` — round 1
- [code-quality/L-3] filterSynthetic allocated a fresh slice even when nothing needed filtering — added a fast path returning the input slice unchanged in the no-synthetic case — round 1
- [rules/L-4] TestIsSyntheticPrompt lacked the `_Scenario_Expected` suffix — renamed to TestIsSyntheticPrompt_MatchesKnownTags — round 1
- [qa/test-gap] Added coverage: all-synthetic Read, multiple interspersed synthetic entries, combined over-cap purge+trim, under-cap leaves-disk-untouched, Compact on missing file — round 1
- [rules/doc] CLAUDE.md internal/panehistory paragraph updated to document the synthetic-turn filter and the Compact race-avoidance rule — round 1

## Dismissed (acknowledged, will not fix; agents may escalate with explicit justification)
- [code-quality/L-oversize] An oversized file whose large entries don't fit keepLast within maxReadBytes is not shrunk on disk — pre-existing behavior unchanged by this diff, reads stay memory-bounded via the tail guard, disk-only edge-of-edge (round 1)
- [qa/minor] Empty-history-list keyboard nav (home/end/pgup/pgdown against 0 entries) has no explicit test — the single post-switch clamp makes `last == -1` safe by inspection and `enter` is guarded by `cursor <= last`; consistent with how the codebase tests neighbouring handlers (round 3)

## Notes
- Round 3 reviewed the follow-up branch `fix/input-history-ui` (PR #117), which fixes the list rendering, scrolling, agent-message filtering and detail viewer. Rounds 1-2 reviewed the original feature.
- `ipc.HistoryEntryMeta` does not match `dto-naming.md`'s three sanctioned suffixes. Pre-existing type name, untouched by these rounds; the rule requires renames to ship in their own commit, so it is deliberately left alone rather than bundled with the fix.
